# Hello World

A minimal C++ project.

## Build

```bash
cmake -B build
cmake --build build
```

## Install

```bash
# System-wide (requires sudo)
sudo cmake --install build

# User installation
cmake --install build --prefix ~/.local
```

## Run

```bash
./build/hello-world
```
